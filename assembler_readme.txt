- To run the assembler you need to have node js installed first.

- This assembler reads a file by the name "code.asm" as an input, and returns the output to a file "instructionMem.txt".

- An example input and output are provided together with the assembler code and a txt file containing the team info.